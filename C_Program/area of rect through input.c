#include <stdio.h>
int main()
{

int length,breath,area;
printf("\nenter the value of length :");
scanf("%d",&length);
printf("\nenter the value of breath :");
scanf("%d",&breath);
area=length*breath;
printf("\n area of rectangle=%d",area);
return 0;
}

